function Speed(props: any) {
  const { handleSpeedChange } = props;
  return (
    <>
      <button onClick={() => handleSpeedChange()}>asdf</button>
    </>
  );
}

export default Speed;

const styles: any = {
  wrapper: {},
};
